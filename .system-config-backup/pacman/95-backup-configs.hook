[Trigger]
Operation = Upgrade
Type = Package
Target = *

[Action]
Description = Backup configs...
When = PostTransaction
Exec = /bin/sh -c 'u="${SUDO_USER:-$(logname 2>/dev/null)}"; h="$(getent passwd "$u" 2>/dev/null | cut -d: -f6)"; [ -n "$h" ] || exit 0; sh "$h/.bin/backup-configs.sh" "$h"'
